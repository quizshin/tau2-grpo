name: CPU checks

on:
  pull_request:
  push:
    branches:
      - master
      - dev

permissions:
  contents: read

concurrency:
  group: cpu-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: tau3-grpo-longhorizon/pyproject.toml

      - name: Install project CPU dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e "./tau2-bench"
          python -m pip install -e "./tau3-grpo-longhorizon[dev]"

      - name: Lint project-owned Python
        run: |
          ruff check tau3-grpo-longhorizon/src tau3-grpo-longhorizon/tests

      - name: Validate shell syntax
        run: |
          bash -n setup.sh
          find tau3-grpo-longhorizon/scripts -type f -name "*.sh" -print0 |
            xargs -0 -n1 bash -n

      - name: Run deterministic CPU unit tests
        run: |
          pytest -q \
            tau3-grpo-longhorizon/tests/test_anchors.py \
            tau3-grpo-longhorizon/tests/test_dynamic_filtering.py \
            tau3-grpo-longhorizon/tests/test_tau_gigpo.py \
            tau3-grpo-longhorizon/tests/test_metrics.py \
            tau3-grpo-longhorizon/tests/test_configs.py \
            tau3-grpo-longhorizon/tests/test_experiment_manifest.py \
            tau3-grpo-longhorizon/tests/test_leakage_audit.py \
            tau3-grpo-longhorizon/tests/test_path_traversal.py \
            tau3-grpo-longhorizon/tests/test_patch_contract.py
