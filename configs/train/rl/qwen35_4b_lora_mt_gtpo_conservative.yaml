includes:
- qwen35_4b_lora_mt_gtpo_smoke.yaml
launch:
  environment:
    RESULTS_DIR: ${CODE_ROOT}/results/runs/mt_gtpo/conservative_smoke_s42
    SWANLAB_EXPERIMENT_NAME: mt-gtpo-conservative-smoke-s42
  overrides:
  - ++algorithm.process_reward.mode=conservative
