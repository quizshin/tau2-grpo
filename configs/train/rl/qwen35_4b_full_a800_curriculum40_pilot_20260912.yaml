# Legacy filename retained for links; ACTIVE pool is 50 tasks (40 core + 10 combinations).
# Pilot: 10 updates. After audit, extend SAME run with TOTAL_UPDATES=100 (6400 rollouts).
# Separate output directory: do not point it at an existing 200-task run.
includes:
- qwen35_4b_full_a800_formal_20260912.yaml
launch:
  environment:
    MODEL_PATH: ${CODE_ROOT}/checkpoints/sft-merged/new-off
    TRAIN_MANIFEST_DIR: ${CODE_ROOT}/results/analysis/rl_curriculum50_20260912/manifests
    RESULTS_DIR: ${TAU3_RUN_ROOT}/rl-curriculum50-20260912/e0_seed42
    GROUPS_PER_UPDATE: '8'
    GROUP_SIZE: '8'
    PPO_MINI_GROUPS: '4'
    TOTAL_UPDATES: '10'
    SWANLAB_EXPERIMENT_NAME: E0-Qwen35-4B-full-c50-g8x8-s42
  overrides:
  - trainer.resume_mode=auto
  - trainer.save_freq=5
  - trainer.max_actor_ckpt_to_keep=2
