# 项目架构与维护规则

## 1. 主仓库与运行环境

- 远程主仓库：`/root/autodl-fs/tau3-core/code`。
- 远程环境：`/root/autodl-fs/tau3-core/environment`。复用对应环境，不在代码目录中重新创建环境。
- 本地对应仓库：`/Users/apple/Projects/program-llm/tau3_grpo_fix/code`，用于编辑、审查及代码同步。
- 项目运行与集成验证默认在远程对应环境进行；本地检查、远程无卡检查和 GPU/FSDP 验证分别说明，不能相互替代。
- 2026-09-18 用户明确要求：需要 GPU 训练、推理、smoke 或续训实验时先停下，列出具体目的和预算，由用户决定是否使用。已有 CPU 验证通过不构成 GPU 授权，历史自动接续授权不覆盖本次任务。
- 远程入口如下；GPU 分配和实验参数仍须核对实际硬件及本次配置，不因激活脚本提供默认值就直接启动训练。

```bash
source /root/autodl-fs/tau3-core/activate.sh
cd "$CODE_ROOT"
```

## 2. 代码架构

以下路径均相对于主仓库。

| 路径 | 职责 |
| --- | --- |
| `configs/` | 模型、硬件、数据及实验配置 |
| `scripts/` | 数据处理、训练、评测和维护入口，保持脚本简洁 |
| `tau3_grpo/training/` | SFT、RL 训练流程、公共正式控制器与服务生命周期 |
| `tau3_grpo/algorithms/` | 优势计算、信用分配和组过滤；`anchors/` 管理状态表示与分组依据 |
| `tau3_grpo/envs/` | 会话、工具执行、环境和模拟器交互 |
| `tau3_grpo/evaluation/` | 任务验证、独立评测与指标计算 |
| `tau3_grpo/analysis/` | 离线审计、轨迹回放与诊断 |
| `tau3_grpo/data/`、`tracking/`、`integrations/` | 数据接口、运行记录与外部系统适配 |
| `verl/` | 训练框架及必要的项目补丁 |
| `tau2-bench/` | 固定版本的基准环境；目录及 Python 包名不能用来判断基准是否过时 |
| `tests/` | 与修改范围对应的回归和集成验证 |
| `env_info/`、`docs/` | 部署环境说明、架构、维护与历史记录 |
| `data/`、`models/`、`checkpoints/`、`results/` | 运行数据、模型、检查点和实验产物，不作为源码副本存放位置 |

- 奖励判定归环境/验证器，信用分配归算法，参数更新归训练框架；不把实验算法写进 shell 启动脚本。
- RL 通过 `algorithm.adv_estimator` 显式选择 `grpo`、`tau_gigpo` 或 `mt_gtpo`。GiGPO 的锚点分组/信号审计与 GTPO 的逐轮过程奖励/优势过滤分别维护；修改共享训练入口时验证三条路径，不能覆盖另一算法的分支。
- 新实验复用现有模块和入口，通过配置选择实现；仅在职责确实不同的时候新增模块。
- `integrations/verl/` 保存框架 estimator/batch 适配，`algorithms/` 保存核心计算；旧 estimator 路径仅转发到同一模块对象。新调用使用公开接口。
- `configs/` 与 `scripts/` 的相似子目录用于参数与入口对应，不是重复代码；公共 Python 实现进入包内。
- 2026-09-18 用户明确：不冻结当前算法或 harness 行为。允许后续修改，但每次行为变化记录版本、假设、旧 buffer 对照和影响，不能覆盖旧实验结果。
- 活动 profile/候选状态索引：`configs/experiments/catalog.yaml`；CPU 分层入口：`scripts/maintenance/check_cpu.py`。新 vendor 修改需更新固定 revision 对照清单 `env_info/vendor_patches.json` 及原因/测试登记。
- 新增代码与实验标准见 `docs/architecture/development_standard.md`；正式单实验入口为 `training/rl/runner.py`，历史控制器保留复现用途。
- 修改 `verl/` 或基准环境时，记录上游 revision、补丁原因和验证证据，不自动升级到上游 `main`。

## 3. 代码维护与实验放置

- 使用 Git 分支和提交管理开发历史，不创建长期维护的 `code-new`、`code-final` 或按日期复制的完整项目。
- 合并旧副本时记录来源分支/提交、保留内容与替代关系，写入现有历史文档；不通过恢复旧目录兼容软链接维持多套代码。
- 不假定本地和远程完全一致。同步前检查双方提交与工作区，保留未提交修改；同步后核对实际变更文件。
- 源码同步不得覆盖或删除环境、模型、数据、检查点及实验结果；不得对整个项目根目录盲目使用删除式同步。
- 新增实验配置时，检查 CLI、启动脚本和训练入口是否支持该配置；配置文件存在不代表实验已经接通。
- 硬件差异通过配置表达；激活脚本不得用通用 `MODEL_PATH` 静默覆盖实验选定的 SFT 初始化模型。
- 每次运行使用独立的 `results/runs/<experiment_id>/<run_id>/`，保存代码版本、实际生效配置、模型/数据身份、日志与产物索引。存在未提交代码时保存对应补丁。
- 改变算法或奖励含义时建立明确的新配置并记录差异，不覆盖已完成实验的配置和结果。实验索引维护在 `EXPERIMENTS.md`。
- 凭据、环境、大模型、数据及大型运行产物不提交到 Git；相关来源和索引用小型文档或清单记录。
- 验证范围与修改相关，不因文档或小配置修改重复启动训练。中断或接管时先检查进程、日志和检查点，避免重复启动。

## 4. 存储与检查点

- 持久盘 `/root/autodl-fs` 保存主代码、环境、数据、实验结果和需保留的 RL 检查点。
- 新实验输出默认写入持久盘下的 `code/results/runs/`，不写入系统盘或数据盘的模型资产目录。
- 数据盘 `/root/autodl-tmp` 和系统盘 `/root` 按下表放置模型资产。代码通过主仓库内的逻辑路径访问，不在算法中硬编码物理位置。

| 主仓库内的软链接 | 实际资产位置 |
| --- | --- |
| `models` | `/root/autodl-tmp/tau3-assets/models` |
| `checkpoints/sft-merged/new-off` | `/root/autodl-tmp/tau3-assets/checkpoints/sft-merged/new-off` |
| `checkpoints/sft-merged/new-on` | `/root/autodl-tmp/tau3-assets/checkpoints/sft-merged/new-on` |
| `checkpoints/sft-merged/old-off` | `/root/tau3-assets/checkpoints/sft-merged/old-off` |
| `checkpoints/sft-merged/old-on` | `/root/tau3-assets/checkpoints/sft-merged/old-on` |
| `checkpoints/sft-comparison` | `/root/tau3-assets/checkpoints/sft-comparison` |

- `checkpoints/` 和 `checkpoints/sft-merged/` 本身是实际目录。上表为当前登记的六个项目资产软链接，修改映射时同步更新文档及迁移记录。
- 不得修改平台软链接 `/root/autodl-fs → /autodl-fs/data`，其当前字面目标为 `../../autodl-fs/data`。
- 迁移前核对容量、真实路径及使用中的进程；先复制并校验内容和必要的加载能力，再切换路径、删除本次迁移旧副本。维护记录写入 `results/maintenance/`。
- 同名文件不代表同一模型，去重须核对内容和来源。保留 LoRA 时记录精确基座、tokenizer 和配置。
- 区分推理权重与完整续训检查点。清理优化器、调度器或其他恢复状态前，明确是否放弃精确续训；不得因模型能加载就认定续训状态完整。
- 内存盘和临时目录不得作为重要产物的唯一保存位置。实例迁移或释放前核验系统盘、数据盘上的独有资产已有可恢复副本，不假定它们与持久盘具有相同保留保证。

- 正式 RL 默认每 10 个外层训练 step 保存一次完整续训检查点，每个实验仅保留最新 1 份。“保存最新的 10 step”指整十节点保存并轮换，不指保留最近 10 份检查点。用户明确调整时以本次要求为准；smoke／短程联调例外须在配置与记录中明确标注。
- 完整续训检查点须包含模型、优化器、学习率调度器、随机状态，以及训练框架恢复步数和数据调度所需的状态；仅有可加载的模型权重不算完整续训检查点。
- 新检查点的各 rank 文件和恢复元数据完整、校验通过后，才清理同一实验的旧检查点。历史节点评测结果、轨迹、日志与配置记录继续保留。
- 正常主动停止应完成下一个整十节点的更新、保存、评测和日志记录后退出。例如在 step 29 请求停止，应完成 step 30 后停止。故障中断单独处理，不能把未完成节点标记为完成。

## 5. RL 相关代码维护

以下四项来自历史实验的实际卡点，仅对本次修改涉及的部分进行验证。

- 修改过滤、batch 划分或损失计算时，保留空 mask、零损失 padding 和各 rank 同步处理，并验证受影响的边界情况。
- 修改 rollout、工具调用或环境交互时，保证同轮多个工具调用完整执行且 observation 正确对应；不同轨迹的会话和可变数据库必须隔离，实际执行与 gold 验证不得共享被修改的数据库状态。
- 修改训练权重到 rollout 的同步、LoRA、检查点或模型导出逻辑时，验证参数映射与实际加载，不能仅以保存成功作为验收依据。
- 修改奖励、优势、分组或过滤算法时，显式区分新旧配置，保留能够重算本次算法结果的输入、分组、mask 和配置记录。

## 6. 正式 RL 训练与对照约定

以下是正式 RL 的默认约定；用户明确调整时以本次要求为准。Smoke／短程联调可使用较小预算及不同保存频率，但必须明确标注，不能作为正式对照结果。

### 6.1 运行记录与续训

- 正式训练启用 SwanLab 在线记录，每个外层训练 step 上传数值指标，横轴使用真实 `global_step`；每 10 step 记录对应评测指标。模式名等字符串放入实验配置或本地记录，不作为数值曲线上传。
- 不同算法、DF 开关或独立实验使用不同 run；同一实验续训沿用原 SwanLab run ID。从 step 30 检查点恢复后，下一训练点为 31，不能重置或人为偏移步数。
- 续训前核对检查点步数、实际生效配置和 SwanLab 已记录步数。若故障造成曲线领先于检查点，先处理记录冲突，不得将重跑结果直接混入原曲线。
- 完成验收须核对 SwanLab 云端记录、检查点及本地日志的一致性，不能仅凭初始化成功或本地上传提示认定记录完整。

### 6.2 评测

- 正式训练默认每 10 step 在固定 selection60 上评测，每任务 4 次，共 240 条轨迹；不默认每 step 额外评测。保留各节点完整结果及异常记录。
- 训练结束后的独立推理评测与训练内评测分别记录；各算法与共同 SFT 起点使用一致的评测协议。
- 官方 final50 在模型名单和协议冻结后使用，不用于反复选择检查点、调整奖励或调参。

### 6.3 与已有 E0–E3 的对照基线

- 新算法若要与已完成的 E0–E3 直接比较，默认匹配：Qwen3.5-4B `checkpoints/sft-merged/new-off` SFT 初始化、非 thinking、全参数 RL、固定 50 任务训练池、seed 42、相同任务调度。
- 每个外层 step 固定采样 8 个任务组，每组 8 条完整轨迹，共 64 条；训练 20 step，共 1,280 条候选轨迹。分别记录外层步数、实际更新次数和过滤后有效样本量。
- 20 step 是上一轮最终确定的对照预算；原先“E0 约 6 小时后向上取整到整十步，其余组匹配步数”是预算确定方式。与历史结果比较时，不因新算法速度不同而重新按各自 6 小时确定步数。
- 对齐学习率 `1e-6`、KL 系数 `0.01`、独立 Qwen3.8-27B-AWQ-INT4 用户模拟器，以及采样参数、上下文和轮数上限。奖励或优势计算的实验差异须显式记录。
- 上一轮部署为 4 张 A800 策略卡加 1 张模拟器卡，采用已验证的 FLA FP32/IEEE、单轨迹 padding 裁剪和 compact head。实际硬件及兼容性仍须核对；更换精度或计算路径时记录差异，重新验证受影响部分。

### 6.4 动态过滤与算法审计

- 新增 MT-GTPO 的动态过滤必须通过配置独立开关；开关对照保持相同初始化、任务调度和候选 rollout 预算。当前采用固定 rollout 过滤，过滤后不补采样。
- MT-GTPO 在联合优势计算后判断整组是否有有效信号，不能仅因终局奖励相同就过滤掉存在过程奖励或步级优势差异的组。保留历史算法配置的原有行为。
- 保存足以重算过程奖励、优势和过滤结果的输入，包括任务／轨迹分组、轮次与 token 区间、奖励明细、过滤前后 mask、优势和配置版本；关键数值统计接入 SwanLab。
- 正式配置应复用已有训练入口和运行管理逻辑。仅新增算法文件或 YAML 不代表保存、评测、续训和停止流程已接通；CPU 测试、真实 GPU 更新和 GPU 续训的验证范围分别说明。

约定来源：任务 `01a08fc8-1e63-7503-a9eb-a067df4e07dc` 中最终确认的保存、评测、预算与 SwanLab 续训要求，以及本次 MT-GTPO 动态过滤要求；于 2026-09-16 整理确认。历史执行证据见 `docs/formal50_fla_execution_20260912.md`，当前参数见 `configs/train/rl/formal50_a800.yaml` 的组件包含链；历史对照输入为 `configs/train/rl/qwen35_4b_full_a800_c50_matched6h_common_20260912.yaml`。其中旧目录和初始预算不覆盖本文件的当前存储约定及最终 20-step 对照预算。

## 7. 文档与交接

- 架构细节参考 `docs/architecture/layout.md`，存储历史参考 `docs/storage.md`，实验索引参考 `EXPERIMENTS.md`，故障与结果影响索引参考 `ERRORS.md`。
- 当前资产拆分证据位于 `results/maintenance/split-assets-20260915/`。旧文档中的目录和部署描述可能已被迁移替代，使用前核对实际路径及后续迁移记录。
- 历史任务、计划参数和文件名不作为当前运行状态的唯一依据；不把尚未执行的计划写成已完成结果。
- 交接说明实际修改、验证范围、正在运行的进程、产物位置和待办。训练完成、导出完成和评测完成分别记录。
- 具体故障沿用已有实验和修复报告，不为一次性故障重复建立规则；新增长期约束应说明它保护的行为及相关证据。
