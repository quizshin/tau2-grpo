# 4B LoRA E1–E3 短程验证 · 2026-09-09

用户在E0完成后授权依次验证E1、E2、E3，全部完成后汇总，再决定正式实验。套件于19:21启动，22:55全部训练与审计完成，三组均通过验收。以下是短程训练诊断结果，不是独立selection评估。

| 编号 | 方法 | 起点与预算 | 状态 |
|---|---|---|---|
| RL-009 | E0：GRPO | SFT-003合并起点，新LoRA；3×2任务×4条 | 已完成，8/24成功；61.05分钟 |
| RL-010 | E1：GRPO＋动态过滤 | 同一起点、seed42、3×2×4 | 已完成，8/24成功；全部验收通过 |
| RL-011 | E2：Tau-GiGPO | 同一起点、seed42、3×2×4 | 已完成，11/24成功；全部验收通过 |
| RL-012 | E3：动态过滤＋Tau-GiGPO | 同一起点、seed42、3×2×4 | 已完成，9/24成功；全部验收通过 |

三组都独立从SFT-003的最佳第24步合并模型新建r16/alpha32适配器，不承接RL-009或前一组的RL权重。冻结BF16基座和视觉模块，只训练约32,464,896个LoRA参数。学习率1e-6，GPU0训练/策略rollout，GPU1运行Qwen3.8-27B-AWQ-INT4模拟器，策略上下文24576，prompt8192＋交互16384，单轮1024，最多15轮，工具观察65536字符。CPU offload、并发、模拟器16k/thinking off等配置沿用RL-009。

每组固定采样24条，任务顺序仍为1072/709、871/760、382/542。E1/E3采用fixed-rollout动态过滤：对组内无奖励差异的轨迹清零训练mask，不额外补采样。整批过滤后没有有效token时跳过actor更新，因此必须分开报告3个候选批次和实际优化器步数，不能把跳过当作一次有效学习。

E2/E3设置omega=1、gamma=0.95、Fnorm=1、最小anchor组大小2，使用structured anchors。Tau-GiGPO使用均值中心化的episode项＋step项；E0/E1保留标准GRPO的组内标准差归一化。因此这是既定方案比较，不能把E2与E0的全部差异只归因于新增step项。验收会另外以omega=0重算已保存批次，量化实际非零step贡献。

## 启动前验证

- 修复 `configs/experiments/arms.yaml` 中E1缺少的 `anchors.mode=structured`。该缺项会导致统一launcher在准备E1时失败。
- 在训练正常返回前显式等待SwanLab结束，避免Ray进程退出时最后一批上传未完成；从Tracking中移除该backend以避免析构时重复finish。没有改变优化器或奖励逻辑。
- 本地79项回归通过（6.43秒），远程79项通过（10.75秒），涵盖四个arm的真实入口dry-run、动态过滤、Tau-GiGPO、telemetry和SwanLab记录。新增断言首次未处理shell输出中的转义逗号而失败；按shell规则解析命令后通过，无对应生产算法故障。
- 对RL-009三份既有debug批次分别应用E1/E2/E3，9组只读CPU算法回放全部有限；它们不是本轮新生成轨迹或新成绩。
- 两份远程源码均验证849个功能文件hash一致。两张A800 80GiB空闲；持久盘约105GiB可用，足够三个约10GiB的最终检查点及写入时新旧两份峰值。

## 控制、验收与证据

套件目录：`/root/autodl-tmp/tau3/runs/qwen35_4b_arms_20260909`；入口 `suite_controller.py`，首次PID94382。按训练→停止本组服务→审计归档→下一组顺序执行；任一步失败会停止推进，先诊断，不重复训练已有run。

每组运行目录分别为 `rl010_4b_lora_e1_20260909`、`rl011_4b_lora_e2_20260909`、`rl012_4b_lora_e3_20260909`，均在上述runs根目录；同名持久目录为 `/root/autodl-fs/tau3_grpo_fix/results/<run>`。完整检查点直接写持久盘 `checkpoints`，保留最近一份完整恢复状态、每步独立LoRA快照；不覆盖E0或SFT资产。每组配置为 `configs/train/rl/qwen35_4b_lora_rl010.yaml` 至 `rl012.yaml`。

套件 `audit_run.py` 检查：三批完成和相同任务schedule哈希；真实优势逐元素重算、过滤mask、GiGPO独立step贡献；全部496个actor LoRA张量到640个vLLM缓冲区映射；云端关键scalar和6条真实预览；最终模型、优化器实际步数、调度器、数据进度与随机状态；持久盘文件hash以及运行资产复制前后hash。没有优势/没有step贡献的结果如实报告，不改奖励。

自动监督每5分钟检查进程、日志、显存、磁盘及套件状态；全部完成后通知用户并暂停。按用户后续授权，三组完成、持久归档与本地证据核验后，核实平台身份并关闭AutoDL的051号实例，保留磁盘数据。按E0循环耗时预计三组约3～4小时，实际取决于轨迹长度、过滤后的更新工作及服务启动。

本轮不运行40/60步正式规模，不运行128条/步，也不使用官方最终50题。正式训练前仍需结合实际成功/失败、过滤有效组、GiGPO信号、稳定性和独立selection方案确定预算。RL-009的GPU0峰值79,755MiB，仅约2.11GiB余量，短程通过不代表任何满长负载都可运行。

## E1已完成（20:34）

RL-010三批成功数4/8、1/8、3/8，共8/24；有效任务组2/2、1/2、2/2，第二批过滤一个全零组，三次actor更新均实际执行。梯度范数0.293079、0.213152、0.211807；完整循环3583.91秒（59.73分钟）。整卡采样峰值GPU0 79,755MiB，GPU1 52,235MiB。

三步保存的优势均与重新计算逐元素一致，退化组mask、496个actor适配器张量到640个vLLM缓冲区映射、优化器/调度器3步及数据进度3均核验通过；云端FINISHED，三步关键指标及6条真实预览匹配，无需补传。持久归档通过，本地同步的73个证据文件逐项hash一致，12个大文件留远程。云实验：[vpl8b1ni](https://swanlab.cn/@quizshin/tau3-grpo-pytrio/runs/vpl8b1ni)。

E0和E1总成功数同为8/24，但第一批分别6/8和4/8；相同seed及任务顺序不保证并发模拟器生成完全一致，不能把批次差异全部归因于过滤。

## E2已完成（21:45）

RL-011三批成功数8/8、1/8、2/8，共11/24；实际actor更新3次，梯度范数0.00977479、0.13419227、0.11535254。完整循环3574.21秒（59.57分钟）。三批GiGPO相对于omega=0的非零step贡献token数为10025、1059、11259，最大绝对差为0.115561、0.551319、0.789688。第一批奖励全1，但折扣后的剩余步数不同，step项仍有非零学习信号。

优势逐元素重算、适配器映射、优化器与调度器步数、云端指标及6条预览、持久归档均通过；本地73个小型证据文件hash核对通过，回执已上传持久结果。云实验：[n5uv04gu](https://swanlab.cn/@quizshin/tau3-grpo-pytrio/runs/n5uv04gu)。11/24为训练轨迹诊断，不是独立评估或方法优越性的结论。

## 用户授权检查点精简

用户要求仅保留必要模型参数，删除冗余检查点。完整检查点先用于上述验收，验收与本地证据核验完成后可删除完整LoRA训练状态、优化器及中间适配器，只保留最终LoRA和精确SFT基座。旧RL-008唯一最终全参模型保留。历史审计回执保留原样，精简后的当前资产以各run的inference-only.json及results/checkpoint_compaction_20260909清理回执为准；精简资产不再提供完整断点续训。E2/E3等全套结束且证据核验完成后统一精简，之后才关闭051。

## E3已完成及最终比较（22:55）

| 方法 | 三批成功数 | 总成功数 | 实际actor更新 | 每批有效组 | 循环分钟 | GPU0 / GPU1峰值MiB |
|---|---|---|---|---|---:|---|
| E0 GRPO | 6、1、1 | 8/24 | 3 | 未启用过滤 | 61.05 | 79755 / 51119 |
| E1 动态过滤 | 4、1、3 | 8/24 | 3 | 2、1、2 | 59.73 | 79755 / 52235 |
| E2 Tau-GiGPO | 8、1、2 | 11/24 | 3 | 未启用过滤 | 59.57 | 79751 / 52237 |
| E3 两者结合 | 7、1、1 | 9/24 | 3 | 1、1、1 | 57.29 | 79751 / 52237 |

E3三批梯度范数0.137961、0.153496、0.181516；非零优势token数7113、5206、8449。相对omega=0的非零step贡献token数4878、1355、5346，最大绝对差0.551803、0.551803、0.663241。三批各过滤掉一个退化任务组，均保留一个有效组，未发生整批跳过。优势逐元素重算、退化组mask、496个actor张量到640个vLLM缓冲区映射、优化器和调度器3步、云端关键指标及6条真实预览、持久归档均通过；本地73项小证据hash匹配。

云实验：[E1](https://swanlab.cn/@quizshin/tau3-grpo-pytrio/runs/vpl8b1ni)、[E2](https://swanlab.cn/@quizshin/tau3-grpo-pytrio/runs/n5uv04gu)、[E3](https://swanlab.cn/@quizshin/tau3-grpo-pytrio/runs/mfchohof)。机器可用性验证通过，三组均有实际非零学习信号；这不足以判定E2性能最好。四组第一次更新前的成功数已不同，且每组仅24条训练轨迹；Fnorm设置也不同。正式规模及独立selection评估等待用户决定，没有自动启动新实验。

机器可读比较见`results/validation/qwen35_4b_arms_20260909/final-comparison.json`。全套审计完成后，E2/E3最终LoRA及共同SFT基座继续保留，完整恢复状态按用户授权精简。关机进度另记录；22:58时Mac锁屏，控制台操作受阻，尚未执行051关机。

最终精简完成：持久盘46.57GiB，数据盘22.86GiB。最终适配器与精确SFT基座保留，完整优化器恢复状态退役，14项清理证据已本地核验。

23:35已在AutoDL控制台核实051实例`857546be50-4fdafb0e`的SSH为`ssh -p 28713 root@connect.nma1.seetacloud.com`，执行正常关机并从平台回读确认“已关机”。磁盘保留，未删除实例。关机完成记录见`results/validation/qwen35_4b_arms_20260909/shutdown-status.json`。平台提示连续关机15天会自动释放实例，数据盘副本需在释放前迁移或保全。
