# Git 发布与 README 更新（2026-09-20）

本次按用户要求发布已有架构改动，并更新 GitHub 项目说明。仓库为 `quizshin/tau2-grpo`，实际默认分支与当前本地分支的上游均为 `main`；不沿用旧 CONTRIBUTING 中失效的远程 master/dev 假设。发布只允许快进，不重写共享历史。

## 提交范围

- 原有 12 个本地未推送提交保留历史，包含版本化 anchors、可见证据、语义抽取与离线审计。
- 运行实现提交 `d0667f5`：262 个文件；三算法公共 runner、分层配置、奖励／优势／过滤、轨迹事实、验证／比较、服务与 checkpoint、veRL 接口和回归／维护工具。
- README 与文档单独提交：更新项目入口、harness、正式协议、指标、工程验收范围、开发标准与实验／错误索引。历史结果继续保留，不把工程通过改写成算法增幅。
- 三个 `verl/verl/experimental/vla/workers/env/` 文件此前被嵌套 ignore 规则遗漏；本次加入已在本地／远程及 vendor 清单中的原始文件，不改变其内容。742 个 runtime／packaging 文件与提交 index 的 SHA256 一致，避免新克隆缺文件。
- 不新增模型、checkpoint、虚拟环境、凭据或大型运行结果；既有明确授权的冻结输入仍保留。待发布内容的凭据模式扫描未发现匹配项，该检查不等于形式化无泄漏证明。

## 发布前验证

- 核心 CPU：379 passed；配置、组件、三算法与兼容默认：72 passed，集合可能重叠，不相加。
- 首次配置检查没有激活环境，shell 找不到 `python`，15 failed / 57 passed；激活 `.venv-cpu` 后同一组 72 passed，未因此修改训练代码。
- 使用固定 Ruff 0.16.6：382 条已登记历史债、0 新增。原本机为 0.16.7，已对齐固定版本，没有放宽基线。
- vendor runtime 清单、shell 语法、README 及入口文档的相对链接与空白检查通过。三处历史日志／Markdown 空白告警保留原文，不改写历史证据。
- 本次不启动 GPU。历史远程 CPU 1302、GPU-only 17及各阶段验收仍按原回执解释。

## 发布与部署回执

GitHub 提交／推送及 Actions 执行状态以仓库实际记录为准，不能用上述本地测试代替托管 CI。小型发布回执、文档前像、源码同步哈希位于运行资产 `results/maintenance/git-publish-20260920/`；该目录不进入 Git。

远程部署只同步本次更改的说明文档和状态配置，逐文件核对前像与后像；既有模型、数据、环境及实验结果保持独立。运行代码已在此前核对中与远程一致。远程 Git 历史若不同则单独处理，不通过 reset 覆盖工作区。
