# 现有训练与验证轨迹盘点（2026-09-18）

远程已有可继续利用的数据，不应在盘点之前断言必须重新采样。本次只读检查已有训练／selection 数据，固定上一轮候选奖励，不拟合权重、不启动训练、不访问 final50。

## 数据清单

| 数据 | 轨迹数 | 当前能支持的检查 |
| --- | ---: | --- |
| MT-GTPO v3 训练 step 1–20 | 1,280 | 完整逐轮过程记录及 MT-GTPO replay，已用于上一轮校准 |
| E0–E3 训练 step 1–20 | 各 1,280，共 5,120 | 对话文本、终局分数、任务和轮次计数；未进入上一轮 IRC，但缺少结构化过程／回放字段 |
| MT-GTPO v3 验证 step 10、20 | 各 240，共 480 | 完整过程记录，可重算奖励；无训练 replay，不是原训练优势回放 |
| E0–E3 验证 step 10、20 | 各 480，共 1,920 | 对话文本与终局分数；同样缺少结构化过程记录 |
| SFT、E0、E1、E3 独立 selection60×4 | 各 240，共 960 | 原始 simulation messages、工具参数、返回、error 标记与终局 reward_info |
| E2 独立 selection60×4 | 239 个已完成结果、1 个异常 | 原始记录可诊断；原汇总 metrics_valid=false，另有独立补测，不能静默混入 |

训练任务池共 50 个，selection 共 60 个，本次核对 task ID 交集为零。
每个训练文件 8 任务×8 轨迹；每个验证文件 60 任务×4 轨迹。
这些都是历史策略轨迹，不是当前候选奖励训练后的样本；不同模型、训练阶段、采样协议须分别报告。

E0–E3 的 `trajectory_json` 实际只有计数、会话／任务 ID、数据库 hash 等摘要，没有 messages 或 turn_records。
不能因为字段叫 trajectory 就当成完整结构化轨迹。原始 `output` 仍有拼接对话文本，后续可评估保守解析／恢复，但缺失的显式错误状态、精确 token 区间和原始 mask／优势不能凭空补齐。
在完成恢复与一致性验证前，这 5,120 条不能直接作为现有严格 IRC 入口的完整 replay buffer。

独立 selection 的 1,199 条已完成轨迹逐条核查：所有 assistant 工具调用都能按 ID 与返回配对，所有返回有布尔 error 标记，未发现空缺 content 或重复 task/trial。
因此适合下一步通过显式适配器重建回合奖励；仍需检查消息顺序、轮次映射和 observation 截断，不能仅凭本次字段齐全就声称重建已验证。
独立评测的生成协议与训练内验证不同，成绩不混算。

## 已完成的固定配方验证

直接使用 `paper_env_conservative_20260918/development/report.json` 中候选权重：gold=1，soft/duplicate=0，state_change=-0.20961006222627246，error=-0.35298915110170204。
阈值 eta=0.1，未按验证结果重新选择权重、阈值或检查点。
首先逐条重算 v3 原始过程奖励，与保存值一致，再应用新配方。

| MT-GTPO 历史验证节点 | 轨迹 | 记录中的成功数 | 未完成官方评分数 | 新配方平均过程奖励–记录终局分数相关性 | 原数值诊断 |
| --- | ---: | ---: | ---: | ---: | --- |
| step 10 | 240 | 118 | 14 | 0.1679330677 | 通过 |
| step 20 | 240 | 104 | 9 | 0.0749255437 | 未通过 eta=0.1 |

两批 gold 平均优势为正、state_change/error 平均优势为负，拟合类别支持数达到 20，unknown 为零。
step20 的唯一诊断失败项是奖励–终局相关性不足。
state_change 出现性相关系数在这两批分别为 -0.155109 / -0.272598，和之前训练留出中 +0.118562 的差异一起保留，不据此重新调权重。

Hybrid 检查按“任务×检查点”的 4 条评测轨迹分组计算，只是反事实诊断，不等同训练的 8 条分组，也不宣称恢复了原验证优势。
相关系数只依赖轨迹平均过程奖励和记录的终局分数，不受此 4 条优势分组选择影响。

本次沿用原日志给提前终止轨迹记录的 0 分，没有把未评分当作官方 DB mismatch：MT-GTPO 训练中有 238 条 scored=false；E0/E1/E2/E3 分别有 307/291/269/278 条。
过程奖励校准受到任务难度、终止原因和历史策略分布影响。检查通过不代表训练会提升，检查失败也不能单凭此处相关性证明旧模型下降由某一奖励项造成。

## 下一步采用现有数据

优先利用记录完整的 MT-GTPO 验证轨迹和独立 SFT／E0–E3 selection 消息检查候选配方的稳定性，保持权重固定。
训练侧优先评估 E0–E3 对话文本的可恢复程度，并明确可恢复奖励分类与无法精确回放的部分，不能伪造 mt_gtpo_replay。
selection 用于现有开发诊断；若后续据此改奖励，就应明确已使用它做开发，不能继续称为未触碰的独立验证集。final50 继续留给协议与模型名单冻结后的最终评测。
本轮结论是“已有数据足够继续诊断，但候选配方尚未稳定通过”，不是“已经取得可正式训练的通过配方”。

## 证据

远程根目录：`/root/autodl-fs/tau3-core/code`。

- 训练：`results/runs/rl-c50-matched6h-a800-20260912/e{0,1,2,3}_seed42/rollouts/`。
- MT-GTPO：`results/runs/mt_gtpo_reference_write_v3/20260917_s42_df0/{rollouts,validation}/`。
- SFT 独立评测：`results/runs/post-rl-selection-20260914/selection/sft/`。
- E0–E3 独立评测：`results/runs/post-rl-selection-recovery-20260914/selection/`。
- 本次脚本、含来源 SHA-256 的完整清单和诊断：`results/analysis/rollout_inventory_20260918/{inspect_buffers.py,report.json,audit.log}`。

本地与远程保留同一份报告和检查脚本。远程在对应项目环境设置 CUDA 不可见并离线运行；无源码或训练配置变更。
