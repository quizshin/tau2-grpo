# 8 卡 5090：A45 冷启动后的 30 step GRPO

## 2026-09-22 最终验收

v4 于北京时间 00:32:53 完成全部 30 步，控制器正常退出。第 10/20/30 步 selection60×4 各 240 条均为完整 trial，成功数分别为 110、108、130（45.83%、45.00%、54.17%）；经验 pass@4 分别为 41/60、43/60、46/60，四次全成功任务数为 13、13、19。第 30 步较 A45 SFT 的 110/240 提升 8.33 个百分点，pass@4 与 SFT 同为 46/60；这是本轮观察到的收益，尚无多种子稳定性证据。

第 30 步终止原因：正常 user_stop 228、轮数上限 11、上下文上限 1；后 12 条按既定协议计零分，未遗漏。所有节点的失败分类与逐条精简结果已归档。

00:39:52 独立验收：本地 metrics 与 SwanLab API 均覆盖 step 1–30；latest=30，最新完整检查点包含 8 rank 的 model/optim/extra_state 及 data.pt 共 25 个必需文件，文件大小与回执一致，PyTorch ZIP 元数据可读。CPU 加载全部 extra_state 验证 scheduler.last_epoch=30 和 CPU/NumPy/Python/CUDA RNG；数据加载器 yielded=30。此为结构与恢复元数据验收，没有额外执行 GPU 恢复训练测试。

产物实际挂载于 `/root/shared-nvme` 的 NFS 共享盘，完整模型、优化器、原始轨迹和日志保留在 `/root/shared-nvme/tau3/runs/grpo_5090_a45/20260921-u30-v4`。小型配置、指标、结果、恢复元数据和校验回执已下载至本地 `results/runs/grpo_5090_a45/20260921-u30-v4`，41 个文件的 SHA256 全部匹配。回执见该目录 `final-verification/verification.json` 与 `local-copy-verified.json`。

2026-09-22 09:01（北京时间）Computer Use 已在 Chrome 页面读到 `grpo-long / ackcs-00gjhp53` 状态“已关机”。本代理没有成功提交关机确认，不推断关机执行者；刷新尝试被用户页面切换中断。后续已停止自动检查，平台状态观察回执保存在 `final-verification/shutdown-status.json`。

**17:49 最新：v1 四策略卡正式尝试失败（0 step），实际准备恢复的是 v2 全八卡策略分片方案。模拟器采样时共享 GPU4–7，训练更新时通过 vLLM sleep API 卸载；以下四策略卡配置保留为失败尝试记录。八卡真实 64 条 batch 重放连续两轮通过，每轮包含两次 PPO optimizer step；不是两个在线 RL step。**

用户于 2026-09-21 明确授权启动 8 卡、30 step GRPO，沿用 A800 训练策略，每 10 step 保存完整状态，仅保留最新一份，并上传 SwanLab。A45/B100 独立评测已完成，本次采用 A45 的第 30 个 SFT 更新后的合并权重。

## 配置与边界

- 起点：`/root/shared-nvme/tau3/runs/sft_coldstart_ab/20260921-AB100-v2/A/merged`。
- 训练池：原 A800 固定 train50，SHA256 `641bde73c1495c59b5c0a87cfc84b9e00c0b5ffd2f86d10fd2205aaf2143adae`。在 `data/manifests/formal50` 独立部署，原 train200 保留；selection60 与训练任务无交集，DB 哈希校验通过。
- 全参数、非 thinking、GRPO、DF 关闭；seed42、lr=1e-6、KL=0.01；每步 8 个任务组，每组 8 条，共 64 条，30 步共 1,920 条训练轨迹。
- PPO 每个 minibatch 4 组（32 条），与 A800 相同；microbatch 每卡 1 条。
- 策略 GPU0–3，独立 27B AWQ 模拟器 GPU4–7、TP4。保留 4 个 FSDP rank，避免为适应 6 个 rank 改变 PPO minibatch。
- FP32 FLA/IEEE，完整工具 schema 与当前 `tau3_token_budget_v1`，策略/用户温度均 0.7。与旧 A800 实验并非所有协议完全相同：起点改为 A45，使用当前 token 协议；明确保留当前会话选定的协议。
- 显存适配：参数、优化器阶段卸载，激活卸载，compact head chunk=64，推理显存比例 0.40。优化器更新仍发生在 GPU；不声称 CPU optimizer。
- 第 10/20/30 步各保存模型、优化器、调度器/RNG、数据调度状态，结构完整后轮换旧检查点，仅保留最新一份。历史评测/轨迹/日志保留。
- 每 10 步 selection60×4（240 条），总 720 条训练内评测轨迹，不使用 final50。SwanLab online，真实 global_step；完整结束后回读云端步数。

## 已完成验证与启动状态

17:05：远程 CPU 29 项通过，包含共享 GRPO/GiGPO/MT-GTPO 配置保持、5090 配置贯通、模拟器设备隔离、无 Git 部署快照排除凭据，以及完整检查点验证。30 步真实 shell/Hydra 预检通过，50 训练题、60 评测题、1,920 训练轨迹。历史 A800 实际 schedule 文件未部署到此机器，预检 `historical_schedule_checked=false`；使用相同池哈希、seed 与现有确定性调度生成，不能把此项写成逐行历史 schedule 现场对比通过。

正式运行目录：`/root/shared-nvme/tau3/runs/grpo_5090_a45/20260921-u30-v1`。已启动控制器 PID35370，入口为现有 `tau3_grpo.training.rl.runner`，新增显式 `--profile`，模拟器设备/TP/端口按硬件配置读取，默认 A800 行为保留。无 Git 的远程部署保存实际源码归档及逐文件哈希，不虚构 Git revision；本地 Git 身份另存 deployment.json。

此前 3 策略卡短测的 v3 仍在首个 backward 出现 Triton CUDA OOM，optimizer 更新 0 次。不能以短测已通过为本次启动依据。本次尚待确认实际非零参数更新、更新后权重同步及 SwanLab 云端点。出现错误时现有控制器会保留现场并清理自有服务，不自动重启、混合不同尝试曲线。

入口配置：`configs/train/rl/formal50_5090_a45.yaml`。源码备份：远端 `bootstrap/grpo30-before-runner.tgz`；本地部署与 patch：`results/runs/grpo_5090_a45/20260921-u30-v1/`。

17:15：四卡模拟器、四个策略 actor/ref 分片和 rollout 服务初始化完成，四份初始卷积权重同步审计已生成。SwanLab online run `3dd0118dd3024fe7a5f54` 初始化成功，独立 API 可读，尚无 step 点；第 1 步真实 64 条采样开始。云端地址：https://swanlab.cn/@quizshin/tau3-grpo-pytrio/runs/3dd0118dd3024fe7a5f54 。

## v1 失败与八卡恢复

17:24：v1 完成首批真实 64 条采样、old/ref 和 GRPO 优势，在 veRL activation_offload.tensor_pop 断言失败。控制器清理全部服务，SwanLab 此 run 没有已完成训练 step；旧日志、batch、源码与云端记录保留。

Actor-only 重放依次确认：关闭框架激活卸载仍触发 Triton OOM；仅释放前向缓存不够；PyTorch save_on_cpu 能越过最初故障，但四个策略 rank 在长轨迹出现实际 PyTorch OOM。没有将这些失败算作在线训练或成功 step。

最后以 8 个 FSDP rank、PyTorch save_on_cpu(pin_memory=True)、保留 HF gradient checkpoint、compact chunk64、前向完成后释放闲置缓存重放同一 64 条真实 batch。两轮各完成 2 次 optimizer.step，所有 rank 的 optimizer step 从 0 到 2 到 4；首轮梯度范数 0.7475699、1.4634135，各 rank 相符且非零有限。首轮 138.7–140.1 秒，次轮 141.7–142.4 秒。首轮 PyTorch allocated 峰值各 rank 18.49–22.41 GiB，不能当作整卡峰值。重放不保存模型，也不纳入新在线曲线。

八卡配置 `formal50_5090_a45_shared8.yaml` 保持实际 64 条与 PPO minibatch32、FP32、种子、学习率、KL及任务池。框架 activation_offload=false；通过现有 model.external_lib 接入项目 CPU tensor 保存适配器。通过现有 agent_loop_manager_class 扩展原生采样管理器，在采样前 wake、完成或失败后 sleep，并读回状态；失败则中止，不带着未释放的模拟器进入更新。模拟器 TP4、GPU4–7、显存0.30，策略使用GPU0–7、显存0.36。检查点验证和恢复按实际 world_size=8。

CPU：新增内存处理与模拟器相位测试、既有正式配置/检查点共36项通过（初次Hydra缺少追加前缀的配置失败已修正后专项通过），CUDA项另行验证。CPU测试覆盖完整梯度/Adam状态一致性、异常时恢复外层保存hooks、sleep/wake幂等、未确认释放时拒绝继续、采样成功/失败均释放，以及硬件共享显式开关。30步真实shell/Hydra预检通过。

另下载 CUDA nvcc13.0.88 仅做同 PTX 的离线编译对照，生成资源与12.9相同，未激活新版编译器、未更改训练环境默认。编译器升级不是本次修复。

18:00：v2 在策略 vLLM 启动失败，未进入在线训练。24,576 上下文至少需要 0.81 GiB KV cache，而显存配额 0.36 仅剩 0.58 GiB；模拟器 sleep 已由 HTTP 状态及显存下降验证。v3 将策略配额调至 0.42，保持协议及八卡更新方案不变。20 项远程 CPU 配置测试通过，启动前预检中。v1/v2 现场保留。

18:05：v3 预检通过，控制器 PID56156 开始加载模拟器。新结果目录 `runs/grpo_5090_a45/20260921-u30-v3`；在线首步尚未产生。

18:13：v3 通过八个策略引擎初始化，真实在线64条采样进行中，模拟器HTTP200。新 SwanLab run `643bef405ee34e2284596`，尚为0个已完成step。策略/模拟器并存时GPU4–7约25.3GB，尚未验证本轮首个更新。

18:21：v3 首批64条真实采样完成（约8分钟）、模拟器sleep、old/ref评分及优势完成，首个backward在FLA dqkwg/prepare_wy核显存不足，控制器失败并清理服务，在线0步。真实批次保留至`v3/update-batches/update_000001.pkl`。额外预留3GiB的八卡actor重放通过部分backward，但后半段PyTorch申请758MiB失败（24.97GiB已分配），证明显存余量不够。正在独立诊断框架原生FSDP2 CPUOffloadPolicy；首次因HF `_no_split_modules`为set触发配置类型不兼容，显式列出原有两类模块后重试，不改vendor。

18:37：FSDP2原生CPUOffloadPolicy＋既有saved-tensor CPU卸载，在v3真实64批次上额外保留3GiB、8rank连续两轮通过，各两次optimizer更新，约121秒/轮。首轮grad norm所有rank同为[1.20906472,1.06077600]，次轮[0.53822023,0.64673370]；PyTorch峰值17.28–20.22GiB（包括人工3GiB）。同一v1批次对照FSDP1：首个范数在2e-5绝对/相对容差内，第二个范数绝对差0.00019014、相对0.012993%，严格断言失败。切换CPU优化器/分片执行存在数值差异，不宣称逐位等价；数学配方、数据及超参数相同。

v4配置`configs/train/rl/formal50_5090_a45_fsdp2.yaml`，21项CPU配置测试和Hydra预检通过。控制器PID69362启动，新目录`runs/grpo_5090_a45/20260921-u30-v4`。八卡GPU负责模型计算，参数及优化器由原生FSDP2卸载CPU；模拟器继续采样共享4–7、更新时sleep。在线首步仍待验收。

18:57：v4 首个完整在线 step 成功，进入step2采样。step1耗时655.86秒（采样475.96、old/ref合计48.24、更新127.20、更新后权重同步3.68秒）；actor grad norm=1.71369535，pg_loss=0.00156983，KL loss=0.01559128。新 SwanLab https://swanlab.cn/@quizshin/tau3-grpo-pytrio/runs/2e4be2f4c0e54b4bb288e ，本地last_logged_step=1，云端已回读step1和非零梯度，16份初始/更新后卷积权重审计全匹配。按首步估算30步纯训练约5.5小时，三次selection评测另计，总计约6.5–8小时；该估计只有单步依据。尚无step10检查点，30步未完成。

## Reward审计（前11步；只读CPU）

704条、88组中57组有终局奖励差异，31组全对/全错；11批优势重算最大误差0，61.02%的生成policy token有非零reward优势。前5步均值44.69%、6–10步47.81%，任务组成变化，不代表控制变量收益。step10与SFT均110/240，经验pass@4为41/60 vs46/60。15条提前终止均是协议允许的完整零分trial，并非缺失评测；更正上一条状态答复的“成绩暂不能有效对比”理由。核心协议一致，执行并发/分片和采样历史不同，未作逐位对齐或严格比较器验收。训练保持不变。详见[只读审计](../results/analysis/grpo_5090_reward_20260921/report.md)。
