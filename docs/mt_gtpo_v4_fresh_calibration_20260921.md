# MT-GTPO split-v4 新 SFT 数据离线校准（2026-09-21）

已使用 15 个原始完整组（120 条）完成 CPU 校准，结果未通过，没有导出冻结训练配方，也没有启动 GPU 或参数更新。用户提到的独立模型评测不混入本次拟合；本次 v4 候选尚未训练，离线奖励相关性不能解释为模型评测提升。

## 输入与初始化

采样目录：`results/runs/v4_shared_irc/20260920_sft_c4_resume/sampling/`。原始完成 121 条，15 个完整组提供 120 条；另 1 条完整轨迹属于未凑齐的组，保留但不进入本次拟合。计划 400 条，剩余 279 个身份已写入 `remaining-identities.json`。原有串行 20 条逐条未变；原始过程奖励、优势和 mask 精确重放通过。

沿用原训练池 40/10 任务划分，当前覆盖其中 12/3 个任务（96/24 条）。门槛 min_support=20、gamma=0.9、lambda_outcome=0.3、DF off 均未改变。

自动校准 CLI 从默认 v4 权重开始，因此未获充分支持的 state_change 保留默认 -0.1。此次复用公开 `calibrate_round` 接口，从采样实际使用的历史候选继续校准，支持不足时保留原 -0.1862388。这是同一批数据的离线重算，不宣称新增 IRC 采样轮次。原自动报告不覆盖。

| 奖励类别 | 采样时权重 | 本次候选 | 本次是否成功重估 |
|---|---:|---:|---|
| 参考数据库写入 gold_write | +0.559738 | +0.534862 | 是，拟合支持数 37 |
| 执行错误 error | -0.352989 | -0.426863 | 是，拟合支持数 23 |
| 非参考写入 state_change | -0.186239 | -0.186239 | 否，支持数 8，保留原值 |

其他类别仍为 0。该候选位于分析报告中，未覆盖生产奖励配置。

## 未通过原因

拟合部分 state_change 仅 8 条存在，低于 20。留出部分有 24 条，记录为成功的仅 1 条；gold_write/error 的支持数均为 1，state_change 从未出现，支持数为 0。支持数是“类别出现、未出现、成功、非成功”四者计数的最小值，并非只数工具调用。

候选在拟合部分的平均 Hybrid 优势：gold_write +0.798992、error -0.443355、state_change -1.524952；即时 proxy 分别 +1.314368、-0.830569、-0.448449。已有观测方向符合预期，但 state_change 样本少，不能认定通过验证。留出上的 state_change 没有观测值。

## 未获官方评分的轨迹

本次 120 条中，94 条获得官方评分，26 条未获得官方评分，训练记录使用回退 0。这类记录不能称为官方确认失败。原完整分组仍保留，候选权重反映当前训练记录口径，属于开发诊断。

其中轮数上限 19 条（拟合 3、留出 16），上下文上限 7 条（均在拟合部分）。

补充诊断发现：官方已评分的拟合切片中，error 平均即时 proxy 为 -0.794705，但平均 Hybrid 为 +0.046811（支持数 19，仍不足 20）；未评分拟合切片的 error Hybrid 为 -1.242144。全量 error 平均为负掩盖了这一切片方向差异。该切片沿用原完整组优势，不是独立重拟合结果；它提示未来回报/终局项叠加仍可能给错误动作正向信用，不能凭全量均值判定已解决，也不能据此断言旧模型评测下降的因果。

补充 `scored_audit.py` 按官方已评分/未评分切片报告，始终先用完整原组算优势，再切片查看；不删行重算组、不用评测数据补标签、不把墙钟中止的 4 条在途尝试当作完整轨迹。

## 后续建议

先核对轮数/上下文截断对拟合的影响，再在明确的同一协议下补齐剩余任务覆盖。完整 400 条也不保证稀有 state_change 或留出成功数达到门槛；需要按实际支持检查。当前不应把此候选作为已校准通过的正式训练配方，也不通过降低门槛、改变留出划分或重复拟合同批数据制造通过结果。

可复现脚本与报告：`results/analysis/v4_fresh_calibration_20260921/`，包含 `run.py`、`report.json`、`summary.json`、`comparison.json`、`per-task.json`、`inputs.json`、`remaining-identities.json` 和评分切片审计。校准前后输入与实现哈希未变，调用冻结接口验证开发数据会被拒绝。
