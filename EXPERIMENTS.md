# 实验记录

更新时间：2026-09-09。范围：本轮 Qwen3.5-0.8B SFT/RL，以及后续4B LoRA SFT/RL + 27B INT4模拟器验证；不是仓库所有历史实验的完整目录。

正在执行：用户已授权E1/E2/E3各3批×8条的短程验证，RL-010→RL-011→RL-012顺序训练和验收；均独立从SFT-003最佳合并模型建立LoRA。正式规模尚未启动，见[E1–E3验证记录](docs/4b_lora_arms_validation_20260909.md)。

最新4B主线：SFT-003完成30次更新，最佳验证loss从0.557704降至0.354217，已恢复第24步并导出。RL-009三步完成、退出0，官方成功6/8、1/8、1/8，总计8/24；三步均有非零奖励优势与梯度，496个actor LoRA张量到640个vLLM缓冲区的映射全部通过。RL训练循环61.05分钟，GPU0峰值79,755MiB；尚无独立评估或RL提升结论。详见[4B主线记录](docs/4b_lora_sft_rl_20260909.md)。

当前结论：LoRA SFT 仍作为 RL 起点。修复后 RL-003 已完成三步，官方成功 3/24，前两步产生真实非零优势和梯度；三个检查点均完成 actor → vLLM LoRA 数值映射核对。但本轮又发现工具观察被默认截到 256 字符，已修正为 65,536 字符。RL-005（新观察预算的 LoRA）三步完成，3/24 官方成功，工具截断归零，出现 2 条上下文超限；RL-004（同预算 full）首批 ref log-prob OOM、未更新；RL-006 仅 offload 仍 OOM。RL-007 分块计算后完成 full 首步，第二步保存时数据盘满；RL-008 从完整首步恢复并完成第 2、3 步，退出 0；full 逻辑链路同为 3/24，仅首步有奖励优势。尚无正式方法优劣结论。

这份文档记录实验目的、设置、结果、证据和决定。完整曲线与案例放 SwanLab，逐步指标和轨迹保留在原始结果目录。目录已重构，本文件继续作为固定入口。

## 1. 实验总览

以下编号是本记录采用的编号，不是 SwanLab run ID。运行状态与结果用途分别标记：进程正常结束不等于实验有效，也不等于模型学到了东西。

| 编号 | 日期 | 阶段 / 方法 | 策略与模拟器 | 运行状态 | 结果与用途 |
|---|---|---|---|---|---|
| SFT-001 | 2026-09-08 | 全量语言参数 SFT | Qwen3.5-0.8B；任务评估用 Qwen3.5-4B | 训练与评估完成 | 最佳 val loss 0.565518；严格重评分 38/240；限于本轮配置比较 |
| SFT-002 | 2026-09-08 | LoRA SFT，r16 | 同上 | 训练与评估完成 | 最佳 val loss 0.529768；严格重评分 54/240；选为后续 RL 起点 |
| RL-001 | 2026-09-08 | E0 / LoRA，r16 | SFT-002 合并模型 + 新 RL LoRA；4B BF16 模拟器 | 三步完成 | 24 条 reward 全零；漏传用户已知信息，仅保留作诊断 |
| RL-002 | 2026-09-08 | E0 / LoRA，r16 | 同 RL-001，补全用户场景 | 三步完成 | 24 条 reward 全零；仍受后续发现的代码问题影响，仅保留作诊断 |
| CHECK-001 | 2026-09-09 | 梯度链路审计、边界回归 | 0.8B LoRA 梯度审计；确定性回复回归测试 | 局部验证完成 | 非零优势可产生参数更新；不代表真实任务成功或完整权重同步通过 |
| RL-003 | 2026-09-09 | 修复后 E0 / LoRA | 0.8B + 4B，同一 SFT 起点；工具观察 256 字符 | 三步完成，退出 0 | 官方成功 3/24；真实非零更新和 LoRA 同步通过；仍含观察截断问题 |
| RL-004 | 2026-09-09 | E0 / full 对照 | 同一 SFT 起点和 4B；观察 65,536 字符 | 首批 OOM，退出 1 | ref log-prob 处显存不足；未完成更新，不纳入奖励对比 |
| RL-005 | 2026-09-09 | E0 / LoRA，观察预算修复 | 同一 SFT 起点和 4B；观察 65,536 字符 | 三步完成，退出 0 | 官方成功 3/24；各步同步通过；无工具截断，2 条上下文超限 |
| RL-006 | 2026-09-09 | E0 / full，actor 状态 offload | 同 RL-004，CPU offload 参数与优化器 | 首批 OOM，退出 1 | 同一 ref log-prob 处失败，未更新 |
| RL-007 | 2026-09-09 | E0 / full，log-prob 分块 | 同 RL-006，减少无梯度概率计算临时张量 | 第一步完整；第二步保存时磁盘满 | 已验证非零 full 更新、473 张量覆盖、视觉冻结；完整 step1 已转存 |
| RL-008 | 2026-09-09 | E0 / full 断点续跑 | 从 RL-007 step1 恢复，检查点直接写持久盘 | 第 2、3 步完成，退出 0 | full 链路 3/24；仅首步有奖励优势；完整最终检查点在持久盘 |
| SFT-003 | 2026-09-09 | Qwen3.5-4B LoRA SFT | 原始4B，45/5对话、5epoch | 30步完成，退出0 | 最佳验证loss 0.354217，第24步恢复核对通过；[新主线记录](docs/4b_lora_sft_rl_20260909.md) |
| RL-009 | 2026-09-09 | E0 / 4B LoRA，27B INT4 模拟器 | SFT-003 合并模型 + 新 LoRA r16 | 三步完成，退出0 | 8/24官方成功；三步均有奖励优势和LoRA同步，工程验证用途 |
| RL-010 | 2026-09-09 | E1 / 4B LoRA＋动态过滤 | 同SFT-003起点＋27B INT4模拟器，fixed-rollout | 三批完成，验收通过 | 8/24成功；有效组2/1/2，实际更新3次，59.73分钟 |
| RL-011 | 2026-09-09 | E2 / 4B LoRA＋Tau-GiGPO | 同SFT-003起点，omega1/gamma0.95/Fnorm1 | 已启动，模拟器启动中 | 3×8短程验证，需核实实际step贡献 |
| RL-012 | 2026-09-09 | E3 / 4B LoRA＋动态过滤＋Tau-GiGPO | 同SFT-003起点、fixed-rollout | E2验收后排队启动 | 3个候选批次×8条，正式预算另行决定 |

范围说明：SwanLab 项目中还存在其他历史实验，本表只纳入本轮有明确执行证据的记录。不能仅根据云端存在实验名称认定本轮跑过对应模型。

## 2. 对比口径

- 模型记录必须区分：原始基座、SFT 导出模型、RL 训练方法、用户模拟器。SFT 用了 LoRA，不意味着后续 RL 必须用 LoRA。
- 本项目的“全量 SFT”是全部语言参数训练，视觉模块冻结；不是多模态全参数训练。
- SFT loss、训练 rollout reward、独立任务评估成功率分别记录。训练 reward 不直接替代评估结果。
- 本轮 SFT 评估预算为 60 个内部 selection 任务 × 每任务 4 次 = 240 次。上下文超限按失败计入分母；只采用隔离数据库后的严格重评分。
- 训练与模型选择使用 AReaL Tau2 风格数据；官方 Tau3 最终 50 个任务不参与训练和选模型。不照搬参考项目的 40/10 划分或 covered/unseen 指标。
- 不同代码版本、模拟器、采样参数、任务集、训练预算或学习率的结果，必须注明差异。本轮 full / LoRA SFT 学习率和参数精度不同，属于两套训练配置比较。
- 表内缺失值写“未记录”或“待测”，不能写成 0；计划值与实测值分开。

## 3. SFT-001 / SFT-002：全量与 LoRA 对比

### 目的与配置

选择 0.8B 策略的 SFT 起点，比较本轮全量与 LoRA 配置的验证损失、任务成功率和显存成本。

共同设置：相同基础模型版本；45 条训练、5 条验证完整对话；seed 42；micro-batch 1、梯度累计 8；5 epoch，共 30 个 optimizer step；上下文上限 24,576；只监督 assistant 内容、工具调用和 EOS。按验证 loss 选择最佳模型。

| 设置 | SFT-001 全量 | SFT-002 LoRA |
|---|---|---|
| 可训练参数 | 752,393,024 | 10,822,656 |
| 学习率 | 2e-5 | 1e-4 |
| LoRA | 不适用 | rank 16 / alpha 32 / dropout 0.05 |
| 精度 | FP32 参数与 Adam 状态，BF16 autocast | BF16 冻结基座，PEFT 可训练参数 |
| 视觉模块 | 冻结 | 冻结 |
| 训练硬件 | 一张 A800 80GB | 一张 A800 80GB，与全量组并行 |

### 实测结果

| 指标 | SFT-001 全量 | SFT-002 LoRA |
|---|---:|---:|
| 初始验证 loss | 0.753222 | 0.751903 |
| 最佳验证 loss | 0.565518 | 0.529768 |
| 最佳 epoch / step | 2 / 12 | 5 / 30 |
| 训练耗时，分钟 | 38.68 | 37.55 |
| 峰值 allocated，GiB | 26.13 | 14.99 |
| 峰值 reserved，GiB | 30.36 | 18.08 |
| 内部 selection 成功 / 完整预算 | 38 / 240 | 54 / 240 |
| 成功率 | 15.83% | 22.50% |
| 上下文超限次数，已计失败 | 12 | 7 |
| 严格回放错误数 | 0 | 0 |

### 结论与限制

本轮选择 LoRA：验证 loss 更低、显存更少，任务成功率高 6.67 个百分点。按任务配对 bootstrap 的“全量减 LoRA”95% 区间为 [-13.75, 0.00] 个百分点，触及零；不能据此声称 LoRA 的任务优势已明确达到统计显著，也不能推广到所有模型大小或最优调参配置。

原评估受注入数据库共享缺陷影响，已使用隔离 DB 重评分。只有 `selection-isolated/` 的完整预算结果用于上表；旧 `selection/` 分数不再采用。最佳全量模型恢复问题也已处理并验证回载 loss。

### 证据与保存状态

- [详细 SFT 对比及修复记录](docs/sft_comparison_20260908.md)。其中 FS 同步与保存路径是历史描述，最新保留状态以下文及清理回执为准。
- SwanLab：[全量 SFT](https://swanlab.cn/@quizshin/tau3-grpo-pytrio/runs/jt7m41hy)；[LoRA SFT](https://swanlab.cn/@quizshin/tau3-grpo-pytrio/runs/x8l5l11u)。指标和样例为训练后补传；补传时源码包含后续修复，不能冒充训练时源码。
- 远程成品：`/root/autodl-tmp/tau3/runs/sft_compare_20260908/{full,lora}/sft_merged_seed42/`。
- 2026-09-09 已删除全量的 checkpoint-12、checkpoint-30 及优化器状态；保留最佳 FP32 权重、两组 BF16 导出、LoRA 适配器、日志与评估结果。已逐字节核验保留的全量最佳权重与 checkpoint-12 的 473 个参数相同。
- [磁盘清理及权重保留证据](results/validation/disk_cleanup_20260909/README.md)。旧优化器状态不能用于精确断点续训，保留的模型可作为新训练起点。

## 4. RL-001 / RL-002：零奖励诊断

### 目的与配置

验证 0.8B 策略 + 4B 用户模拟器下的真实 RL 执行链路；这两轮是短程工程诊断，未进行正式训练效果比较。

共同设置：E0 GRPO；SFT-002 合并权重上新建 LoRA rank 16 / alpha 32；学习率 1e-6；seed 42；每次更新 2 个任务组、每组 4 条 rollout；共 3 次更新、24 条轨迹。GPU 0 承担策略训练与 rollout，GPU 1 承担 Qwen3.5-4B BF16 用户模拟器，模拟器关闭 thinking。

RL-002 补全了传给模拟器的原生用户场景，包括 known_info。策略起点、任务清单与主要训练参数保持和 RL-001 一致。

### 实测结果

| 指标 | RL-001 | RL-002 |
|---|---:|---:|
| 完成更新流程次数 | 3 | 3 |
| 轨迹数 | 24 | 24 |
| 非零官方 reward 轨迹数 | 0 | 0 |
| GRPO advantage | 全 0 | 全 0 |
| actor grad norm | 全 0 | 全 0 |
| adapter 权重变化 | 未观察到，检查点哈希相同 | 未观察到，检查点哈希相同 |
| GPU 0 峰值，nvidia-smi MiB | 65,157 | 65,155 |
| GPU 1 峰值，nvidia-smi MiB | 49,233 | 49,235 |
| 进程退出码 | 0 | 0 |

RL-002 耗时约 29 分 32 秒；失败分类为轮数上限 18 条、工具错误 3 条、任务结果错误 3 条。显存为整卡 nvidia-smi 观测，不能与 SFT 的 PyTorch allocated / reserved 直接混比。

### 诊断与结论

1. RL-001 漏传 known_info 等信息，模拟器可能给出错误身份信息；这轮不能作为完整任务协议验证。
2. RL-002 修复场景传递后仍全零；随后又发现最后允许的助手轮次可能未执行/记录，以及工具调用伴随正文未完整进入评分历史的问题。
3. 2026-09-09 已修复这些边界和记录问题。由于 RL-002 早于本次修复，不能将其作为“代码问题已排除，因此 0.8B 能力不足”的证据。
4. 两轮都没有证明有效 RL 学习，也没有验证非零 LoRA 更新后的完整 actor → vLLM 同步。

### 证据与保存状态

- [RL-001 验证报告](results/validation/rl_qwen35_20260908/validation-report.json)。报告中的 checkpoint 存在状态和云状态是当时快照。
- [RL-002 验证报告](results/validation/rl_qwen35_scenariofix_20260908/validation-report.json)；[实际启动参数](results/validation/rl_qwen35_scenariofix_20260908/launch-config.json)。
- [代码修复、测试和远程同步状态](results/validation/rollout_fixes_20260909/README.md)。运行源码补丁已同步到远程数据盘，6 个文件哈希一致；远程回归测试在 50 秒限时内未完成，不能记为通过。
- SwanLab 的 `bd743csc`、`8qja7qdf` 及旧失败尝试 `vwg965wg` 已按要求删除；不再提供失效实验链接。[删除回执](results/validation/rollout_fixes_20260909/swanlab-deletion-receipt.json)。
- RL-001 的数据盘 global_step_2/3 已清理；本地轨迹、诊断报告和审计证据保留。RL-002 系统盘检查点不在此次数据盘清理范围内；其当前存在性需再次检查，不能将旧报告视为实时库存。

## 5. CHECK-001：修复与链路验证

- 本地相关回归测试 123 项通过；边界模块最终单独检查为 12 项通过。后者不是额外 12 次真实训练。
- 回归用确定性的模型与用户回复，驱动真实工具、会话及官方评分；覆盖最后工具执行、最后用户 STOP、工具正文、Qwen2.5 Hermes JSON 与 Qwen3.5 XML 格式。
- 独立 GPU 梯度审计使用合成非零 GRPO 优势：梯度非零、LoRA 参数变化、冻结参数不变，验证了所测条件下的优化链路。
- 这些检查均不能代替修复后的真实任务 RL 重跑。

证据：[边界修复](results/validation/rollout_fixes_20260909/README.md)；[梯度审计](results/validation/rl_qwen35_scenariofix_20260908/gradient-audit/README.md)。

## 6. 后续计划与历史启动准备

2026-09-09 接管检查：原实例 SSH 可达，但处于无卡模式且没有训练进程；两份远程源码的原部署清单 1,425 个文件全部匹配，SFT 起点与 0.8B/4B 模型文件均在。已新增 `configs/train/rl/qwen35_lora_rl003.yaml` 和配套模拟器配置，保留 RL-002 的三步预算、显存分配及 offload 设置，独立保存本轮结果和权重同步审计。连接、启动与验收方法见 [接管记录](docs/training_handover_20260909.md)。以上为开卡前的历史准备；开卡后的 RL-003 / RL-005 实测结果已在本文及[对照记录](docs/rl_validation_comparison_20260909.md)填写。

以下保留接管时的计划顺序；最新运行状态以上方总览为准。正式评估和更大模型仍需单独冻结预算。

| 优先级 | 实验 | 固定条件 / 主要变量 | 需要回答的问题 |
|---|---|---|---|
| 1 | RL-003：修复后短程验证 | 保留 RL-002 的模型、SFT 起点和任务预算，只更新已修复代码 | 真实工具/评分链路是否正常？是否出现非零优势、梯度和权重变化？ |
| 2 | 全量 RL 与 LoRA RL 对比 | 固定 SFT 起点、模拟器、数据和预算，记录各自学习率及显存 | 哪种配置在同等评估下效果和成本更合适？ |
| 3 | 更大策略模型 / 量化模拟器 | 模型大小和模拟器变化单独登记，不混入方法对比 | 任务能力及吞吐提升能否覆盖显存成本？ |

RL-003 / RL-005 已验证真实非零 LoRA 更新及完整数值同步，但多数任务组仍缺少奖励差异。后续应结合失败分布和独立 selection 评估决定扩展方向；9B 策略和新量化模拟器组合尚无本轮效果结论。

## 7. 新实验填写模板

复制本节到对应阶段，先登记再启动。失败、重跑和更换配置使用独立编号，不覆盖旧结果。

### 实验编号：待填写

| 字段 | 内容 |
|---|---|
| 日期、运行状态、结果用途 | 待填写；如“完成 / 仅诊断” |
| 目的与对照组 | 待填写 |
| 相对对照组的改动 | 待填写；列出所有变化 |
| 策略模型 / revision / SFT 起点 | 待填写；附权重 hash 或对应清单 |
| 训练方法 | SFT 或 RL；full 或 LoRA；算法；可训练范围；rank / alpha |
| 用户模拟器 | 模型、revision、量化、thinking、采样参数、服务配置 |
| 数据与预算 | 数据版本/hash、split、任务数、seed、group size、batch、step/epoch、上下文与轮数限制 |
| 学习率、精度、硬件 | 待填写；注明 GPU 分配、offload 和并行设置 |
| 代码与环境版本 | Git commit + 未提交改动/源码快照 hash；依赖锁定文件 |
| 复现入口 | 实际启动命令、配置快照、显式覆盖项；不写密钥 |
| SwanLab | 项目、run ID、链接、phase/group；标记离线、补传或已删除 |
| 本地/远程证据 | 指标 JSON、日志、轨迹、模型路径；注明保留或清理状态 |

**结果：** SFT 填训练/验证 loss、最佳 step；RL 填 reward 分布、全零/全一组比例、优势、梯度、KL、有效权重更新及失败原因。共同记录耗时、吞吐、峰值显存及统计口径。独立评估写成功数/完整尝试数、任务与采样预算，不能只报百分比。

**案例：** 记录 task ID、step、成功/失败、关键工具动作、实际结果及完整轨迹链接；至少抽查成功和失败案例（若无成功则明确记录），不要把 SFT 参考答案当模型生成结果。

**判断：** 分开写已观察到的事实、原因假设、验证证据、结论局限和下一步。是否进入正式对比、是否作为后续起点，要给出理由。

**更新历史：** 每次补传、重评分、修复或清理追加日期与影响；保留原 run 身份，注明哪些旧分数或模型不能再使用。

## 8. 维护与证据位置

本文件目前人工维护，并已根据现有报告回填；尚未实现训练结束后自动更新 Markdown。SwanLab 和机器可读指标继续由现有记录代码写入。

原外层验证材料现已迁入本项目 `results/validation/`，链接已更新；模拟器与 RL 混放材料已分开。详见 [结果归档索引](docs/experiments/results_index.md)。`results/` 仍为 Git 忽略的运行资产，单独克隆源码不会附带这些证据，需要另行同步。

2026-09-09 已按职责重构本地源码目录，历史证据仍保留原始运行时的路径与参数；部署结果见架构迁移记录。远程运行目录和 autodl-fs 旧副本的状态分别记录，不能将“已同步数据盘源码”写成“持久存储已同步”。

### 2026-09-09 RL-003 启动记录

12:44 双 A800 80GB 已确认空闲；Torch 2.11.0+cu130、vLLM CUDA 扩展及两卡实际运算通过。已核对 837 个功能源码文件；部署预检发现此前 Git 忽略规则漏掉 3 个未使用的上游 VLA 文件，已补齐两份源码后重验。远程相关回归 103 passed（50.52 秒，退出 0）。实验控制器保存于数据盘 `runs/rl003_boundaryfix_20260909`，独立管理模拟器、训练、GPU 监测及退出状态。本轮结果尚未产出，不预先认定有效学习。

RL-003 第 1 步：官方 reward 分布 1×1、7×0，优势范围 [-0.499999, 1.499997]，梯度范数 0.44476199。检查点 LoRA B 非零元素 6,397,515；按 TP=1 的分块、BF16 和 alpha/r=2 映射后，372 个 actor LoRA 张量全部覆盖，vLLM 480 个缓冲区（含未启用的零槽）全部 hash 匹配。第 2 批使用更新后的策略生成。这证明了本批真实非零更新及同步，不等于任务成功率已经提升。云实验：[3stth2pz](https://swanlab.cn/@quizshin/tau3-grpo-pytrio/runs/3stth2pz)。

### RL-003 完成与观察预算修复

三步 mean reward 为 0.125 / 0.25 / 0，梯度范数为 0.444762 / 0.451977 / 0.000804；第三步优势全零，小梯度来自含 KL 的目标，不能计为奖励带来的新学习信号。共 3/24 官方通过；失败包括轮数上限 12、工具错误 5、任务结果错误 4。第三步 adapter 仍可能因正则项与优化器状态变化，不能用文件 hash 变化代替非零 reward advantage。

每个已保存步骤的 372 个 actor LoRA 张量均覆盖，按 BF16、alpha/r=2 及 vLLM 分块映射后 480 个缓冲区 hash 全部相同。整卡峰值 GPU0 65,157 MiB、GPU1 49,731 MiB。SwanLab 状态 FINISHED；3 步关键 scalar 与 6 条实际预览正文已从云端回读核对。保留 step2/3 完整检查点和 step1 单独适配器快照；持久副本为 `/root/autodl-fs/tau3_grpo_fix/results/rl003_boundaryfix_20260909`。

观察审计发现 200 个训练任务的 1,503 次参考工具返回中，1,283 次超过原 256 字符限制，最大 35,912 字符；回放全部无工具错误，且只操作新建的内存数据库。新默认 65,536 字符覆盖这些参考返回，总 token/轮数预算不变。对应本地回归 67 passed（20.56 秒）；远程 67 passed（48.61 秒）。[修复说明](docs/observation_budget_20260909.md)。

RL-004 / RL-005 均使用新观察预算、同一 SFT 起点、同一 3×2×4 任务顺序、seed42、lr1e-6 和 GPU1 的 4B BF16 模拟器（0.60 分配）。full 使用 FP32 语言参数，LoRA 使用 BF16 冻结基座；两者是具体配置比较。full 仅保留最近一个完整检查点，LoRA 保留最近两个；检查点留存不同不改变优化计算。RL-003 显式保留旧 256 字符上限作为诊断对照，不与新 full 混成单变量方法比较。

### RL-005 完成、RL-004 启动

RL-005 三步官方成功数为 3/8、0/8、0/8；梯度范数 0.716165、0.000682、0.000742，后两步奖励优势为零。工具截断标记从 RL-003 的 107 处降为 0，但总成功仍为 3/24，出现 2 条上下文超限。峰值 GPU0 65,155 MiB、GPU1 49,305 MiB。每步 372 个 actor 张量映射至 480 个 vLLM 缓冲区全部 hash 匹配；SwanLab FINISHED，三步指标及 6 条预览正文核对通过。

14:08 已启动 RL-004 full 对照，启动前两卡空闲、839 个功能源码文件核验通过。详细设置、逐步耗时和对话事实错误案例见[对照记录](docs/rl_validation_comparison_20260909.md)。

RL-004 在首批 ref log-prob 的 BF16 log-softmax 处 OOM（需新增 15.16 GiB，可用 12.32 GiB），未完成更新，训练退出 1；原 SwanLab 失败身份 `i7utseim` 已在后续云端清理中删除，持久盘证据保留。14:20 启动独立 RL-006，仅开启 actor 参数/优化器 CPU offload，源码清单 840 文件验证通过；预算、精度、学习率和官方评分保持不变。

### RL-006 / RL-007 显存修复与存储恢复

RL-006 在首次更新前的参考 log-softmax 处 OOM（请求 15.16 GiB，可用 4.13 GiB）。RL-007 对无梯度、三维低精度 logits 按 128 token 分块，保留 autograd 路径和任务/token 预算。本地相关回归 103 passed；远程 100 passed / 3 skipped（未使用模型的 tokenizer 资产缺失）。CUDA BF16/FP16、有/无 autocast 四组输出完全一致；受控探针的 BF16 autocast 临时峰值从 128 MiB 降至约 24 MiB。功能源码清单 841 文件通过核验。

RL-007 首步成功 3/8、梯度范数 5.187435；320 个语言参数张量变化，153 个视觉张量保持不变。第二步计算结束后，保存优化器时数据盘满，退出 1。保留 1 个检查点的实现仍需新旧两份同时存在，初期空间估计不足。完整 step1 和不完整 step2 已转存持久盘，67 文件、15,894,737,283 字节全部 SHA256 核验；不完整 step2 只供诊断，不能恢复。

RL-008 从完整 step1 回载模型、优化器、调度器、随机状态及数据进度；检查点直接写持久盘。全参本轮仅 E0/GRPO，E1～E3 尚未执行。完整结果见[对照记录](docs/rl_validation_comparison_20260909.md)。

### RL-008 续跑完成（15:29）

全参逻辑链路 RL-007 step1 + RL-008 step2/3 的成功数为 3/8、0/8、0/8；梯度范数 5.187435、0.008580、0.005719。后两步奖励优势全零。最终参数审计覆盖全部 473 张量，320 个语言张量变化、153 个视觉张量不变，全部有限。GPU0 整卡采样峰值 80,851 MiB，GPU1 49,305 MiB。SwanLab 第 2、3 步指标和 4 条实际预览核对通过。

由于失败的第 2 批额外采样并重跑，完成链路按 24 条统计，实际 RL-007/008 debug 轨迹 32 条；此前两次 OOM 的成本另计。不得据此宣称 full/LoRA 方法优势或严格效率差异。E1～E3 和独立 selection 评估尚未开展。最终资产及恢复方式见[接管记录](docs/training_handover_20260909.md)。

### 2026-09-09 整盘清理收尾

按用户要求完成远程三盘清理，实际增加可用空间约 19.24 GB。六处数据盘重复权重/检查点逐文件 SHA256 核对后改为持久副本链接；RL-007 不完整 step2 删除，保留失败文件清单、历史 hash 及日志。完整 step1、最新 step3 与全部模型/实验记录保留，最终模型和 SFT 起点 hash 重验通过。当前库存与证据见[清理记录](docs/server_cleanup_20260909.md)，旧归档回执仅代表历史时点。

### 2026-09-09 SwanLab RL 列表清理

已归档并删除两次首次更新前 OOM 的云实验：RL-004 `i7utseim`、RL-006 `nqslriqa`。重新查询确认 RL 从 7 个减至 5 个；保留 RL-003、RL-005、RL-007/008 以及旧 TRIO 4B `0uk586zt`。旧 4B 的“尚未训练”描述过时，实际已有指标与对话，故保留；不计入本轮 veRL 对比。历史失败日志、配置、归档回执均保留，详见[云实验清理记录](docs/swanlab_cleanup_20260909.md)。

### 2026-09-09 21:45：E2验证完成，E3启动，检查点精简获授权

RL-011（4B LoRA，Tau-GiGPO）3×8条完成，成功8/8、1/8、2/8，共11/24；实际优化器更新3次，总循环59.57分钟。三批均验证非零step项贡献，优势重算、actor/vLLM映射、云端与持久归档通过，本地73项小证据hash匹配。RL-012自动启动。详见[方案验证记录](docs/4b_lora_arms_validation_20260909.md)。

用户授权仅保留必要模型参数，删除冗余完整LoRA检查点、优化器及中间适配器并去重原始基座；最终适配器与精确基座、旧RL-008最终全参模型及小型证据保留。精简后不再提供完整断点续训，历史验收文件不代表精简后仍有完整恢复状态；以新增inference-only.json和清理回执为准。运行中E3不清理，全套验收后最终精简并核实关闭051。

### 2026-09-09 22:55：E1/E2/E3全套验证通过

RL-010/011/012均完成24条训练轨迹和3次真实actor更新，成功数8/24、11/24、9/24；完整循环耗时59.73、59.57、57.29分钟。E1有效组2/1/2，E3有效组1/1/1，没有整批跳过；E2/E3各批均验证独立GiGPO step贡献非零。三组优势重算、mask、actor/vLLM适配器映射、优化器步数、云端及持久归档审计通过，各73项本地小证据hash匹配。两张卡已无训练/模拟器显存占用。正式实验未启动，短程训练成绩不能作为独立效果排名。

详情及云端链接见[最终比较](docs/4b_lora_arms_validation_20260909.md)。最终检查点已按用户授权精简，持久盘46.57GiB。23:35在AutoDL控制台核实051实例`857546be50-4fdafb0e`的SSH地址一致后，正常关机并从平台回读“已关机”，保留磁盘。无需再启动训练、精简或旧完整检查点审计。
